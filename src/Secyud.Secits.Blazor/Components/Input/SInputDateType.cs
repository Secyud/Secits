namespace Secyud.Secits.Blazor;

public enum SInputDateType
{
    /// <summary>Lets the user enter a date.</summary>
    Date,
    /// <summary>Lets the user enter both a date and a time.</summary>
    DateTimeLocal,
    /// <summary>Lets the user enter a month and a year.</summary>
    Month,
    /// <summary>Lets the user enter a time.</summary>
    Time,
}