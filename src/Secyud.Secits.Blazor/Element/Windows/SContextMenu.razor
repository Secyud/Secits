@namespace Secyud.Secits.Blazor.Element
@inherits SStyledBase

@if (HideMode || _visible)
{
    <div @ref="Ref" class="@GetClass()" style="@GetStyle()" @onclick:stopPropagation>
        @ChildContent
    </div>
}